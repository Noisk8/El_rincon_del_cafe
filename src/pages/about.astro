---
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import { Image } from "@astrojs/image/components";
import { authors } from "@data/authors";
import Pagetitle from "@components/pagetitle.astro";
import Link from "@components/ui/link.astro";
---

<Layout title="Nosotros">
  <Container>
    <Pagetitle>
      <Fragment slot="title">Acerca de nosostros</Fragment>
      <Fragment slot="desc">El Rincón del cafe</Fragment>
    </Pagetitle>

    <div class="grid md:grid-cols-3 gap-10 mx-auto max-w-4xl mt-12">
      {
        authors.slice(0, 2).map((item) => (


          <div class="group">
            <div class="w-full aspect-square">
              <Image
                src={item.image}
                format="avif"
                width={580}
                height={580}
                alt="Team"
                class="w-full h-full object-cover rounded transition  group-hover:-translate-y-1 group-hover:shadow-xl"
              />
            </div>

            <div class="mt-4 text-center">
              <h3 class="text-sm text-gray-500"> {item.name}</h3>
            </div>
          </div>
        ))
      }
    </div>

    <div class="mx-auto max-w-xl text-center mt-14">
      <p class="text-lg leading-relaxed text-gray-600">
        El Rincón del Café es un lugar acogedor con decoración rústica, música suave y opciones variadas de café. Ideal para compartir momentos agradables en compañía de amigos y familiares.
      </p>
      <div class="mt-8">
      </div>
    </div>
  </Container>
</Layout>
